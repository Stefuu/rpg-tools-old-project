/** @jsx React.DOM */

var React = require('react');

var Quietus = React.createClass({

  render: function() {
    return (
      <div className="quietus">
        <span>
          Quietus é bla bla.
        </span>
      </div>
    );
  }
});

module.exports = Quietus;
